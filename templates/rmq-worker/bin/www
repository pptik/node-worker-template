console.log(``)
async function main() {
  try {
    let conn = await require('../setup').connectToRmq()

    require('../services/consumer').consume(conn)
  } catch (error) {
    console.log(error)
  }
}

main()